import { Box, Button, Container, Grid, Stack, Typography } from '@mui/material';
import { ArrowRight, HeartHandshake, Leaf, Users } from 'lucide-react';
import { Link } from 'react-router-dom';
import hero from '../../assets/hero.png';
import { database } from '../../services/database';

export function ConsumerAbout() { const settings = database.getSettings(); return <Container component="main" maxWidth="lg" sx={{ py: { xs: 6, md: 10 } }}><Grid container spacing={{ xs: 5, md: 8 }} alignItems="center"><Grid size={{ xs: 12, md: 6 }}><Box component="img" src={hero} alt="Inside our restaurant" sx={{ width: '100%', height: { xs: 340, md: 560 }, objectFit: 'cover', borderRadius: 6 }} /></Grid><Grid size={{ xs: 12, md: 6 }}><Stack spacing={3}><Typography color="primary" variant="overline" fontWeight={900}>OUR STORY</Typography><Typography component="h1" variant="h2" fontWeight={900}>Food made with care, served with clarity.</Typography><Typography color="text.secondary" fontSize="1.08rem">{settings.restaurantName} brings thoughtful recipes, dependable ingredients, and modern service together in {settings.address}. Our team prepares every order through one connected kitchen workflow.</Typography>{[[Leaf, 'Fresh ingredients', 'Recipes are linked to live inventory so availability stays honest.'], [Users, 'People-first service', 'Our dining room and online guests share the same attentive team.'], [HeartHandshake, 'Local hospitality', 'Simple ordering lets us spend more time caring for every plate.']].map(([Icon, title, body]) => <Stack key={title} direction="row" spacing={2}><Icon color="#c2410c" /><Box><Typography fontWeight={900}>{title}</Typography><Typography variant="body2" color="text.secondary">{body}</Typography></Box></Stack>)}<Button component={Link} to="/menu" variant="contained" endIcon={<ArrowRight />} sx={{ alignSelf: 'flex-start' }}>Explore our menu</Button></Stack></Grid></Grid></Container>; }
