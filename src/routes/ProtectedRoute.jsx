import { Box, CircularProgress } from '@mui/material'; import { Navigate, useLocation } from 'react-router-dom'; import { useAuth } from '../hooks/useAuth'; import { roleHomePath } from './access';
export function ProtectedRoute({ children, allowedRoles }) { const { user, isReady } = useAuth(); const location = useLocation(); if (!isReady) return <Box sx={{ minHeight: '100vh', display: 'grid', placeItems: 'center' }}><CircularProgress aria-label="Loading session" /></Box>; if (!user) return <Navigate to="/login" replace state={{ from: location.pathname }} />; if (!allowedRoles.includes(user.role)) return <Navigate to={roleHomePath[user.role]} replace />; return children; }
