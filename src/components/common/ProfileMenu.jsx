import { Avatar, Box, Button, IconButton, Menu, MenuItem, Stack, Tooltip, Typography } from '@mui/material'; import { LogOut } from 'lucide-react'; import { useState } from 'react'; import { useAuth } from '../../hooks/useAuth';
export function ProfileMenu() { const { user, logout } = useAuth(); const [anchor, setAnchor] = useState(null); if (!user) return null; return <><Tooltip title="Account"><IconButton onClick={(event) => setAnchor(event.currentTarget)} aria-label="Open account menu"><Avatar sx={{ width: 34, height: 34 }}>{user.name.charAt(0)}</Avatar></IconButton></Tooltip><Menu anchorEl={anchor} open={Boolean(anchor)} onClose={() => setAnchor(null)}><Box sx={{ px: 2, py: 1, minWidth: 220 }}><Typography fontWeight={900}>{user.name}</Typography><Typography variant="caption" color="text.secondary">{user.email} · {user.role}</Typography></Box><MenuItem component={Button} onClick={() => { setAnchor(null); logout(); }} sx={{ width: '100%', justifyContent: 'flex-start' }}><Stack direction="row" spacing={1}><LogOut size={18} /><span>Sign out</span></Stack></MenuItem></Menu></>; }
